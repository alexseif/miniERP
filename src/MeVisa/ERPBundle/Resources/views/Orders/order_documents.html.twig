<div class="panel panel-default">
    {# FIXME: wrap long file names   #}
    <div class="panel-heading">
        <span class="panel-title">Documents</span>
    </div>
    <div class="panel-body">
        <div class="row">
            {%for document in documents%}
                <div class="col-xs-12 col-md-6">
                    <a href="{{ document.path}}" class="thumbnail" target="_blank">
                        {% if document.path ends with 'pdf' %}
                            <span class="glyphicon glyphicon-file"></span>
                        {% else %}
                            <img src="{{ document.thumbnail ? document.thumbnail : document.path }}" alt="{{ document.name }}" width="100%"/>
                        {% endif %}
                        <span style="word-wrap: break-word;">{{ document.name }}</span>
                    </a>
                </div>
            {% endfor %}
        </div>
    </div>
</div>