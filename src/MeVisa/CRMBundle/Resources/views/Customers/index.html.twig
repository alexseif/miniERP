{% extends ':default:admin.html.twig' %}

{% block content %}
    <!-- Page Heading -->
    <div class="row">
        <div class="col-md-12">
            <h3 class="page-header">
                Customers <small> {{ entities|length }} </small>
            </h3>
        </div>
    </div>
    <!-- /.row -->
    <!-- page content -->
    <div class="row">
        <div class="col-md-12">
            <div class="panel panel-default">
                <div class="panel-body">
                    <div class="table-responsive">
                        <table class="table table-bordered table-hover table-striped records_list" id="customers-list">
                            <thead>
                                <tr>
                                    <th>Name</th>
                                    <th>Email</th>
                                    <th>Phone</th>
                                    <th>Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for entity in entities %}
                                    <tr>
                                        <td>{{ entity.name }}</td>
                                        <td>{{ entity.email }}</td>
                                        <td>{{ entity.phone }}</td>
                                        <td>
                                            <a class="btn btn-primary" href="{{ path('customers_show', { 'id': entity.id }) }}">show</a>
                                            <a class="btn btn-primary" href="{{ path('customers_edit', { 'id': entity.id }) }}">edit</a>
                                        </td>
                                    </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- /.row -->
    <a class="btn btn-primary" href="{{ path('customers_new') }}">Create a new entry</a>
{% endblock %}
