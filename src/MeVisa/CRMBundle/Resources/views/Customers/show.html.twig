{% extends ':default:admin.html.twig' %}

{% block content %}
    <div class="panel panel-default">
        <div class="panel-heading">
            <span class="panel-title">Customer</span>
        </div>
        <div class="table-responsive">
            <table class="table table-bordered table-hover table-striped records_list firstCol25">
                <tbody>
                    <tr>
                        <th>Name</th>
                        <td>{{ entity.name }}</td>
                    </tr>
                    <tr>
                        <th>Email</th>
                        <td>{{ entity.email }}</td>
                    </tr>
                    <tr>
                        <th>Phone</th>
                        <td>{{ entity.phone }}</td>
                    </tr>
                </tbody>
            </table>
        </div>
        <div class="panel-footer">
            {# TODO: the following links #}
            {#<a class="btn btn-primary" href="{{ path('customers_edit', { 'id': entity.id }) }}">Edit</a>#}
            {#{{ form(delete_form) }}#}
            <a class="btn btn-warning" href="{{ path('customers') }}">Back</a>
            {# TODO: the following link #}
            <a class="btn btn-default" href="#">Orders</a>
        </div>
    </div>
{% endblock %}
