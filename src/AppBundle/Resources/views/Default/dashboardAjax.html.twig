<div class="row">
    <div class="col-sm-12">
        {% set orders = backoffice %}
        {% set panelTitle = 'Backoffice Orders' %}
        {% include 'AppBundle:Default:dashboardPanel.html.twig' %}
    </div>
</div>
<div class="row">
    <div class="col-sm-6">
        {% set orders = pending %}
        {% set panelTitle = 'Pending Orders' %}
        {% include 'AppBundle:Default:dashboardPanel.html.twig' %}
    </div>
    <div class="col-sm-6">
        {% set orders = document %}
        {% set panelTitle = 'Document Required Orders' %}
        {% include 'AppBundle:Default:dashboardPanel.html.twig' %}
    </div>
</div>
<div class="row">
    <div class="col-sm-6">   
        {% set orders = post %}
        {% set panelTitle = 'Post Orders' %}
        {% include 'AppBundle:Default:dashboardPanel.html.twig' %}
    </div>
    <div class="col-sm-6">
        {% set orders = completed %}
        {% set panelTitle = 'Completed Today' %}
        {% include 'AppBundle:Default:dashboardPanel.html.twig' %}
    </div>
</div>